version: '3'
jenkins:
    image: jenkins/jenkins:lts
    volumes:
        - /var/run/docker.sock:/var/run/docker.sock
    ports:
        - "8080:8080"
        - "50000:50000"
    user: root
    restart: "no"